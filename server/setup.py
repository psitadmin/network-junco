from setuptools import setup, find_packages

setup(name='junco', version='1.0', packages=find_packages())
# To launch venv:
#   - WIN:          .\venv\Scripts\activate
#   - Linux/MacOS:  source ./venv/bin/activate
# 
# Then, go to root path (server) and "pip install -e ."